import { View } from "react-native";
import { Text } from "react-native"

const LoginScreen = () => {
    return(
        <View>
            <Text>This is a LoginScreen</Text>
        </View>
    );
}

export default LoginScreen;

